describe('ImageModal', () => {
  it('should render current description provided', () => {});

  it('should provide default description if one is not provided', () => {});
  it('should run the onCancel prop function when user clicks Close button', () => {});
});
